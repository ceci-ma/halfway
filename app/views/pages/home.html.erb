<div
  id="map"
  style="width: 100%;
  height: 600px;"
  data-markers="<%= @markers.to_json %>"
  data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"
></div>

<div class="container border">

  <div class="row justify-content-center home-box">
      <div class="col-10 form-container">
        <%= simple_form_for :search, url: venues_path, method: :get do |f| %>
<!--         <div style="height: 50px;">
          <h2 id="banner-typed-text" style="display:inline; "> Enter</h2>
        </div> -->
          <div class="form-group">
            <%= f.input :location, label: "Start Point", class: ""%>
          </div>
          <div class="form-group">
            <%= f.input :location, label: "End Point" %>
          </div>
          <%= f.submit "Search venues", class: "btn btn-home btn-block" %>
        <% end %>
    </div>
  </div>
</div>
